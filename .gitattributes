# Hook scripts must use LF line endings, or the shebang breaks on macOS/Linux
# (a CRLF turns "#!/usr/bin/env bash" into an invalid interpreter path).
hooks/session-start text eol=lf
hooks/run-hook.cmd  text eol=lf
